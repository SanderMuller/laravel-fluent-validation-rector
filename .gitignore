# Dependencies
/vendor
auth.json

# Caches
.cache
/build

# IDE Helper
_ide_helper.php
_ide_helper_models.php
.phpstorm.meta.php

# Editors
/.idea
/.fleet
/.vscode

# Misc
phpstan.neon
testbench.yaml
/coverage
composer.lock
.phpunit.result.cache

SPEC_*.md
PEER_FEEDBACK.md

# Pre-release drafts (notes flow to GitHub release body, never to git history)
/internal

# Rector skip log — written by parallel workers, inspectable post-run.
# The .session sentinel is a PPID-keyed marker used to coordinate
# per-run log truncation across parallel workers.
.rector-fluent-validation-skips.log
.rector-fluent-validation-skips.log.session

# Dogfood regression baselines from peer instances — saved here so
# they're reachable from the worktree but kept out of git history.
.dogfood-baselines/
.claude/scheduled_tasks.lock

# Autoresearch artefacts (synthetic corpus, benchmarks, progress logs).
/autoresearch/
# >>> boost (managed) >>>
# Generated by boost-core. Edit .ai/ instead.
.agents/skills/
.boost/
.claude/commands/
.claude/skills/
.config/boost/
.github/prompts/
# <<< boost (managed) <<<
